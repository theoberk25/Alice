# Google ADK cloud agent (build 01) dependencies.
# ADK pulls in a v1 `mcp` (FastMCP client) transitively, matching the Light MCP
# server's pinned v1 line. Verified against these versions on Python 3.14.
google-adk>=2.8,<3
google-genai>=2.22,<3
