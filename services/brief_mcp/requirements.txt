# Decision-Brief MCP server deps. See docs/agent-build/04-brief-mcp.md.
#
# Pin the MCP SDK to v1: `pip install mcp` now resolves to 2.x, where FastMCP
# was renamed to MCPServer and server.py's `mcp.server.fastmcp.FastMCP` import
# no longer exists. v1 1.29.x serves Streamable HTTP at /mcp by default
# (settings.streamable_http_path). Verified against the same pin as the Light MCP.
mcp<2
pyyaml>=6

# The alice source (BRIEF_SOURCE=alice) will add an HTTP client + the console
# wire schema once agreed; not needed for BRIEF_SOURCE=mock.
