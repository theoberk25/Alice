# Package tooling

[sign_package.py](../../packages/tooling/sign_package.py) and
[verify_package.py](../../packages/tooling/verify_package.py) are existing empty
placeholders. They are preserved at their original paths and are not runnable
signing or verification implementations. Coordinate ownership before replacing
these placeholders; new standalone package utilities belong in this folder.
